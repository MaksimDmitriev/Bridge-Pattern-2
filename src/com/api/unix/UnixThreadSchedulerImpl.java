package com.api.unix;

import com.api.ThreadScheduler;
import com.api.ThreadSchedulerImpl;

public class UnixThreadSchedulerImpl extends ThreadSchedulerImpl {

    public UnixThreadSchedulerImpl(ThreadScheduler threadScheduler) {
        super(threadScheduler);
    }
}
