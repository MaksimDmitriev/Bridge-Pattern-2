package com.api.windows;

import com.api.ThreadScheduler;
import com.api.ThreadSchedulerImpl;

public class WinThreadSchedulerImpl extends ThreadSchedulerImpl {

    public WinThreadSchedulerImpl(ThreadScheduler threadScheduler) {
        super(threadScheduler);
    }
}
